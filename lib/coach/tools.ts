import type Anthropic from "@anthropic-ai/sdk";

// Tool definitions for the Garden Coach. Empty for now — filled in as
// capabilities (zone lookup, plant search, garden CRUD, etc.) come online.
export const coachTools: Anthropic.Tool[] = [];
