-- Garden Coach — Supabase seed file.
-- Runs automatically after migrations on `supabase db reset`.
--
-- \ir resolves paths relative to this file's location, so the include
-- below works whether you run `supabase db reset` from the project
-- root or pipe this file into psql from elsewhere.

\ir seed_plants.sql
